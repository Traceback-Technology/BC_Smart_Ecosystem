const Food = require('../models/foodModel');
const { foodSchema } = require('../middlewares/validator');
exports.getFoods = async (req,res,next) => { try { const filter={}; if(req.query.category && req.query.category!=='All Items') filter.category=req.query.category; if(req.query.popular==='true') filter.popular=true; if(req.query.available!=='all') filter.available=true; if(req.query.search) filter.name={$regex:req.query.search,$options:'i'}; const foods=await Food.find(filter).sort({popular:-1,createdAt:-1}); res.json({success:true,count:foods.length,data:foods}); } catch(e){next(e);} };
exports.getFood = async (req,res,next) => { try { const food=await Food.findById(req.params.id); if(!food)return res.status(404).json({success:false,message:'Food item not found'}); res.json({success:true,data:food}); } catch(e){next(e);} };
exports.createFood = async (req,res,next) => { try { const {error,value}=foodSchema.validate(req.body); if(error)return res.status(400).json({success:false,message:error.details[0].message}); const food=await Food.create(value); res.status(201).json({success:true,data:food}); } catch(e){next(e);} };
exports.updateFood = async (req,res,next) => { try { const food=await Food.findByIdAndUpdate(req.params.id,req.body,{new:true,runValidators:true}); if(!food)return res.status(404).json({success:false,message:'Food item not found'}); res.json({success:true,data:food}); } catch(e){next(e);} };
exports.deleteFood = async (req,res,next) => { try { const food=await Food.findByIdAndDelete(req.params.id); if(!food)return res.status(404).json({success:false,message:'Food item not found'}); res.json({success:true,message:'Food item deleted'}); } catch(e){next(e);} };
