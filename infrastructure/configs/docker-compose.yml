version: '3.9'

services:

mongodb:
image: mongo:7
container_name: bc-smart-mongodb
restart: always
ports:
- "27017:27017"

```
volumes:
  - mongodb_data:/data/db
```

mqtt:
image: eclipse-mosquitto
container_name: bc-smart-mqtt
restart: always
ports:
- "1883:1883"

volumes:
mongodb_data:
