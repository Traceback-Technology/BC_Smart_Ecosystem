const router=require('express').Router(); const c=require('../controllers/activityController'); const {identifier}=require('../middleware/identification'); router.get('/',identifier,c.getActivities); module.exports=router;
