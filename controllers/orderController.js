const Order = require('../models/orderModel'); const Food = require('../models/foodModel'); const Activity = require('../models/activityModel'); const { orderSchema } = require('../middlewares/validator');
const orderNo = () => `BC-${Math.floor(1000 + Math.random()*9000)}`;
exports.createOrder = async (req,res,next) => { try { const {error,value}=orderSchema.validate(req.body); if(error)return res.status(400).json({success:false,message:error.details[0].message}); let items=[],subtotal=0; for(const line of value.items){ const food=await Food.findById(line.foodId); if(!food||!food.available)return res.status(400).json({success:false,message:`Food item ${line.foodId} is unavailable`}); items.push({foodId:food._id,name:food.name,price:food.price,quantity:line.quantity,notes:line.notes}); subtotal += food.price*line.quantity; } const deliveryFee=0; const order=await Order.create({orderNumber:orderNo(),userId:req.user.userId,items,subtotal,deliveryFee,total:subtotal+deliveryFee,deliveryAddress:value.deliveryAddress,deliveryNotes:value.deliveryNotes,estimatedArrival:new Date(Date.now()+30*60*1000)}); await Activity.create({userId:req.user.userId,type:'food',title:items[0].name,amount:order.total}); res.status(201).json({success:true,message:'Order placed',data:order}); } catch(e){next(e);} };
exports.getOrders = async (req,res,next) => { try { const orders=await Order.find({userId:req.user.userId}).sort({createdAt:-1}); res.json({success:true,count:orders.length,data:orders}); } catch(e){next(e);} };
exports.getOrder = async (req,res,next) => { try { const order=await Order.findOne({_id:req.params.id,userId:req.user.userId}); if(!order)return res.status(404).json({success:false,message:'Order not found'}); res.json({success:true,data:order}); } catch(e){next(e);} };
exports.updateStatus = async (req,res,next) => { try { const allowed=['pending','preparing','ready','out_for_delivery','delivered','cancelled']; if(!allowed.includes(req.body.status))return res.status(400).json({success:false,message:'Invalid status'}); const order=await Order.findByIdAndUpdate(req.params.id,{status:req.body.status},{new:true}); if(!order)return res.status(404).json({success:false,message:'Order not found'}); res.json({success:true,data:order}); } catch(e){next(e);} };
exports.getTracking = async (req,res,next) => { try { const order=await Order.findOne({_id:req.params.id,userId:req.user.userId}).select('tracking status estimatedArrival'); if(!order)return res.status(404).json({success:false,message:'Order not found'}); res.json({success:true,data:order}); } catch(e){next(e);} };
exports.updateTracking = async (req,res,next) => { try { const order=await Order.findByIdAndUpdate(req.params.id,{$set:{tracking:req.body}},{new:true,runValidators:true}); if(!order)return res.status(404).json({success:false,message:'Order not found'}); res.json({success:true,data:order}); } catch(e){next(e);} };
exports.updateNotes = async (req,res,next) => { try { const order=await Order.findOneAndUpdate({_id:req.params.id,userId:req.user.userId},{deliveryNotes:req.body.deliveryNotes},{new:true}); if(!order)return res.status(404).json({success:false,message:'Order not found'}); res.json({success:true,data:order}); } catch(e){next(e);} };
exports.cancelOrder = async (req,res,next) => { try { const order=await Order.findOne({_id:req.params.id,userId:req.user.userId}); if(!order)return res.status(404).json({success:false,message:'Order not found'}); if(['delivered','cancelled'].includes(order.status))return res.status(400).json({success:false,message:'Order cannot be cancelled'}); order.status='cancelled'; await order.save(); res.json({success:true,message:'Order cancelled',data:order}); } catch(e){next(e);} };
